namespace at_prueba_tecnica_backend.Application.Features.DTOs;

/// <summary>
/// Generic DTO for combobox/dropdown lists (key-value pairs).
/// </summary>
public record ComboBoxDto(
    string Id,
    string Label
);
